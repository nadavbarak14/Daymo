---
title: Smoke
description: Two-scene fixture used by the single-capture integration test.
url: http://localhost:9999
viewport:
  width: 800
  height: 600
mocks:
  - source: inline
    routes:
      "GET /api/me": { "name": "Alex" }
      "GET /api/projects": []
---

# Welcome

Welcome to the demo. This is the first scene.

```playwright
await page.waitForSelector("[data-testid=new-project-btn]");
```

---

# Open the dialog

Now we open the new-project dialog.

```playwright
await fx.cursorTo("[data-testid=new-project-btn]", "the new project button");
await page.click("[data-testid=new-project-btn]");
await page.waitForSelector("[data-testid=new-project-dialog][open]");
```
