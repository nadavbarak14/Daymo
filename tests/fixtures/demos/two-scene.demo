---
title: Smoke
description: Two-scene fixture used by the E2E smoke test.
url: __WILL_BE_REPLACED__
viewport: { width: 800, height: 600 }
__MUSIC_LINE__
mocks:
  - source: inline
    routes:
      "GET /api/me": { "name": "Alex" }
      "GET /api/projects": []
---

# Welcome

Welcome to the demo. This is the first scene.

```playwright
await page.waitForSelector("[data-testid=new-project-btn]");
```

---

# Open the dialog

Now we open the new-project dialog.

```playwright
await fx.step("Open the dialog");
await fx.cursorTo("[data-testid=new-project-btn]");
await page.click("[data-testid=new-project-btn]");
await page.waitForSelector("[data-testid=new-project-dialog][open]");
```
